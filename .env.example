# Requerido: URL de conexión a PostgreSQL
# Railway lo genera automáticamente al agregar el plugin de PostgreSQL
DATABASE_URL=postgresql://usuario:contraseña@host:5432/nombre_db

# Puerto del servidor (Railway lo asigna automáticamente)
PORT=3000

# Entorno
NODE_ENV=production
